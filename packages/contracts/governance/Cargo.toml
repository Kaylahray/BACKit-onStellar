[package]
name = "governance"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk.workspace = true

[features]
testutils = ["soroban-sdk/testutils"]
